mixin checkbox-toggle(options)
  - const { id, checked } = options;

  .checkbox-toggle
    input.checkbox-toggle__checkbox(
      type="checkbox",
      id=id,
      checked=checked
    )
    label.checkbox-toggle__label(for=id) Получать спецпредложения
