mixin button-submit(options)
  - const { text } = options;

  .button-submit
    input#submit.font-h3.button-submit__input(type="submit", value=text)
    label.button-submit__label(for="submit") 
      span.font-icon.button-submit__icon arrow_forward
