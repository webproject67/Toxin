mixin button-like(options)
  - const { text, number, active } = options;
  - const classNameActive = active ? 'button-like_active' : '';

  button(
    class=`button-like js-button-like ${classNameActive}`,
    type="button"
  )= text
    span.button-like_font_another= number
