mixin button-like(options)
  -
    var className = "button-like"
    className += options.className ? ` ${options.className}` : ""
  button(class= className type="button")= options.text
    span.button-like--number= options.number

mixin button-rate(options)
  ul.button-rate
    - for (var x = 0; x < options.number; x++)
    li(class="font-icon font-lg button-rate__item") star
    - for (var x = 0; x < 5 - options.number; x++)
      li(class="font-icon font-lg button-rate__item") star_border

mixin button-link(options) 
  -
    var className = "font-h3"
    className += options.className ? ` ${options.className}` : ""
  a(class= className)= options.text

mixin button-submit(options) 
  .button-submit
    input(class="font-h3 button-submit__input" type="submit" id="submit" value= options.text)
    label(class="button-submit__label" for="submit") 
      span(class="font-icon button-submit__icon button-submit__icon--color") arrow_forward
