mixin slide(options)
  -
    const {text, desc} = options

  .slide
    .slide__texts
      h3(class="font-h3 slide__text")= text
      h3(class="font-h3 slide__text slide__text--color") 5 000₽ - 10 000₽
    .slide__slide 
    p.slide__desc= desc